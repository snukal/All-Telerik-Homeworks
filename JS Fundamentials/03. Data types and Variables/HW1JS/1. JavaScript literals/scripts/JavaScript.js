var integer = 1390;
jsConsole.writeLine("The integer is: " + integer);

var float = 973.152558355865;
jsConsole.writeLine("The float is: " + float);

var str = 'So far, so good!';
jsConsole.writeLine("The string is: " + str);

var boolean = false;
jsConsole.writeLine("The boolean is: " + boolean);

var array = ['Somthing', 959.333, 2, { Name: 'Tarkan', Songs: 33 }];
jsConsole.writeLine("We have an array: " + array);

var obj = {
    Name: 'Tarkan',
    Points: 20,
    Performer: 'Nencho'
};
jsConsole.writeLine("Finaly an objekt: " + obj);
